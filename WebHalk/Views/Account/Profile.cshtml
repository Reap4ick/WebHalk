@model WebHalk.Models.ProfileViewModel

<div class="container mt-5">
    <h2>Your Profile</h2>
    <div class="card">
        <div class="card-body">
            <p><strong>Email:</strong> @Model.Email</p>
            <p><strong>Username:</strong> @Model.UserName</p>
        </div>
    </div>
</div>
